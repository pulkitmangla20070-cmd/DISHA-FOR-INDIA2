/**
 * Validation skeleton for listing certificates.
 */
const validateGetCertificates = (req, res, next) => {
  return next();
};

/**
 * Validation skeleton for getting a certificate by ID.
 */
const validateGetCertificate = (req, res, next) => {
  return next();
};

/**
 * Validation skeleton for verifying a certificate.
 */
const validateVerifyCertificate = (req, res, next) => {
  return next();
};

module.exports = {
  validateGetCertificates,
  validateGetCertificate,
  validateVerifyCertificate,
};
